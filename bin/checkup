#!/usr/bin/env bash

APPNAME="$(basename $0)"
USER="${SUDO_USER:-${USER}}"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# @Author      : Jason
# @Contact     : casjaysdev@casjay.net
# @File        : checkup
# @Created     : Mon, Dec 31, 2019, 00:00 EST
# @License     : WTFPL
# @Copyright   : Copyright (c) CasjaysDev
# @Description : check for updates on Arch
#
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Main function

checkup() {
  DIR="${BASH_SOURCE%/*}"
  if [[ ! -d "$DIR" ]]; then DIR="$PWD"; fi
  if [[ -f "$DIR/functions.bash" ]]; then
    source "$DIR/functions.bash"
  else
    echo -e "\t\tCouldn't source the functions file"
    exit 1
  fi

  [ ! "$1" = "--help" ] || printf_help "Usage: checkup    |   Check for pacman updates"

  ping -q -c 1 1.1.1.1 >/dev/null || exit 1

  if cmd_exists pacman; then
    notifications "📦 Repository Sync" "Checking for package updates..."
    sudo pacman -Syyuw --noconfirm || notifications "Error downloading updates."

    #Check your internet connection, if pacman is already running, or run update manually to see errors."
    if [ -n "${STATUSBAR}" ]; then pkill -RTMIN+8 "${STATUSBAR:?}"; fi

    if pacman -Qu | grep -v "\[ignored\]"; then
      notifications "🎁 Repository Sync" "Updates available."
    else
      notifications "📦 Repository Sync" "Sync complete. No new packages for update."
    fi

  fi
}

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# execute function
checkup "$@"
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
